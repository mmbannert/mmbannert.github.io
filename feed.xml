<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://mmbannert.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mmbannert.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-03T11:17:12+00:00</updated><id>https://mmbannert.github.io/feed.xml</id><title type="html">Dr. Michael M. Bannert</title><subtitle>This is the personal webpage of Michael Bannert, a brain and vision scientist. </subtitle><entry><title type="html">“Memory color influences conscious object perception” finally accepted</title><link href="https://mmbannert.github.io/blog/2026/memory-colour-consciousness-paper-accepted/" rel="alternate" type="text/html" title="“Memory color influences conscious object perception” finally accepted"/><published>2026-07-27T12:30:00+00:00</published><updated>2026-07-27T12:30:00+00:00</updated><id>https://mmbannert.github.io/blog/2026/memory-colour-consciousness-paper-accepted</id><content type="html" xml:base="https://mmbannert.github.io/blog/2026/memory-colour-consciousness-paper-accepted/"><![CDATA[<p>Our paper on how memory colour affects visual consciousness has finally been accepted by “Neuroscience of Consciousness”. Led by our PhD student Vincent Plikat, the study investigated how prior knowledge about which colours are typically associated with which objects affects visual awareness. To answer this question, he examined how colour pairs, red versus green or blue versus yellow, compete for visual awareness in binocular rivalry. Crucially, these colours were super-imposed on photos of objects that were typical of one of the colours. Observers may thus have been presented a strawberry in red, its typical colour, to one eye and in green to the other eye. From the observer’s perspective, their perception keeps alternating between seeing the red strawberry and the green strawberry as is common in binocular rivalry. Now, would observers be more likely or less likely to perceive the object in its typical colour, i.e., its memory colour? It turns out that, by and large, numerous behavioural metrics showed a shift in conscious report in the direction of the congruent object-colour pair, i.e., the red strawberry, rather than the incongruent, green one.</p> <p>These findings are important for at least two reasons. First, previous psychophysical studies of memory colour effects on perception relied on self-reported judgements, which made them prone to decision biases, i.e., people’s reports may not reflect their perception but rather just how they judge things to look. The new study, in contrast, uses binocular rivalry, which minimises attention and judgement confounds. At a more general level, the paper thus addresses the fundamental question of <em>cognitive penetrability</em>, i.e., if cognition can influence perception. Second, a large body of literature suggests that the brain is a <em>prediction machine</em> that uses prior knowledge to interpret sensory signals perceptually. It is less clear, what this view entails for perception: On the one hand a violation of visual expectations may carry more information than expectations that are confirmed by sensory input and may thus prioritise their perception. Simply put: if something is already expected, there is nothing “newsworthy” when that expectation bears out. On the other hand, a match between expectations and visual input may strengthen the plausibility of that interpretation and thereby strengthen its perception. It turns out that the latter is the case for memory colour.</p> <p>The final publication can be accessed on the <a href="https://doi.org/10.1093/nc/niag055">webpage of Neuroscience of Consciousness</a>. A previous, pre-print version of the paper is <a href="https://osf.io/preprints/psyarxiv/8uzjd_v1/">available for download on OSF</a>.</p>]]></content><author><name></name></author><category term="publication"/><category term="consciousness"/><category term="colour"/><category term="psychophysics"/><summary type="html"><![CDATA[Our paper on how memory colour affects visual consciousness has finally been accepted by “Neuroscience of Consciousness”. Led by our PhD student Vincent Plikat, the study investigated how prior knowledge about which colours are typically associated with which objects affects visual awareness. To answer this question, he examined how colour pairs, red versus green or blue versus yellow, compete for visual awareness in binocular rivalry. Crucially, these colours were super-imposed on photos of objects that were typical of one of the colours. Observers may thus have been presented a strawberry in red, its typical colour, to one eye and in green to the other eye. From the observer’s perspective, their perception keeps alternating between seeing the red strawberry and the green strawberry as is common in binocular rivalry. Now, would observers be more likely or less likely to perceive the object in its typical colour, i.e., its memory colour? It turns out that, by and large, numerous behavioural metrics showed a shift in conscious report in the direction of the congruent object-colour pair, i.e., the red strawberry, rather than the incongruent, green one.]]></summary></entry><entry><title type="html">Keep record of archived directory trees with `ncdu` and `zstd`</title><link href="https://mmbannert.github.io/blog/2026/keep-record-of-archived-directory-trees-with-ncdu-and-zstd/" rel="alternate" type="text/html" title="Keep record of archived directory trees with `ncdu` and `zstd`"/><published>2026-05-22T10:50:00+00:00</published><updated>2026-05-22T10:50:00+00:00</updated><id>https://mmbannert.github.io/blog/2026/keep-record-of-archived-directory-trees-with-ncdu-and-zstd</id><content type="html" xml:base="https://mmbannert.github.io/blog/2026/keep-record-of-archived-directory-trees-with-ncdu-and-zstd/"><![CDATA[<h1 id="keeping-a-record-of-archived-directory-trees-with-ncdu--zstd">Keeping a record of archived directory trees with <code class="language-plaintext highlighter-rouge">ncdu</code> + <code class="language-plaintext highlighter-rouge">zstd</code></h1> <p>Every now and then IT archives a chunk of my data to a safer drive and tells me I can delete my local copy to free up space. That’s fine in principle — but the moment the directory is gone, I lose any sense of <em>what</em> was in it and <em>how</em> it was organised. So before deleting anything I want a small, browsable record of the tree that I can keep around indefinitely and later use to tell IT “please reinstate <code class="language-plaintext highlighter-rouge">X/Y/Z</code> from the backup.” What makes this approach so pleasant is that the record isn’t just a flat text listing: it lets me re-open the directory exactly as it stood at the moment of backup and wander through it interactively — folder by folder, sizes and all — long after the data themselves are gone.</p> <p>Two tools do this nicely:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">ncdu</code></strong> (“NCurses Disk Usage”) scans a directory and can export the whole tree — every file and folder, with sizes — as a compact JSON dump. Crucially, it can later re-open that dump in its interactive browser <em>without the original data being present</em>.</li> <li><strong><code class="language-plaintext highlighter-rouge">zstd</code></strong> (Zstandard) compresses the dump. Directory listings share enormous common prefixes, so they shrink dramatically — a tree of millions of files becomes a file of a few tens of MB at most.</li> </ul> <p>Together they turn an entire directory tree into a small file I can browse, grep, and archive next to the data it describes.</p> <h2 id="setup">Setup</h2> <p>I keep <code class="language-plaintext highlighter-rouge">ncdu</code> in its own conda environment so it doesn’t clutter anything else. A single command both creates the environment and installs <code class="language-plaintext highlighter-rouge">ncdu</code> into it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> ncdu-archive-viewer <span class="nt">-c</span> conda-forge ncdu
</code></pre></div></div> <h2 id="capture-the-tree--before-i-delete-anything">Capture the tree — <em>before</em> I delete anything</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ncdu <span class="nt">-x</span> <span class="nt">-o</span> - /path/to/dir | zstd <span class="nt">-19</span> <span class="nt">-f</span> <span class="nt">-o</span> backup-index.ncdu.zst
</code></pre></div></div> <p>What the flags do:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-x</code> keeps the scan on a single filesystem.</li> <li><code class="language-plaintext highlighter-rouge">-o -</code> writes the export to stdout, which I pipe straight into <code class="language-plaintext highlighter-rouge">zstd</code>.</li> <li><code class="language-plaintext highlighter-rouge">-19 -f</code> compresses hard and overwrites any stale file of the same name.</li> </ul> <h2 id="convince-myself-its-all-there">Convince myself it’s all there</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>zstdcat backup-index.ncdu.zst | ncdu <span class="nt">-f-</span>
</code></pre></div></div> <p>The command first uses <code class="language-plaintext highlighter-rouge">zstdcat</code> to decompress the backup file and pipes it to <code class="language-plaintext highlighter-rouge">ncdu</code> for reading. (The trailing dash in <code class="language-plaintext highlighter-rouge">-f-</code> means that <code class="language-plaintext highlighter-rouge">ncdu</code> is reading the file from stdin.) This drops me into <code class="language-plaintext highlighter-rouge">ncdu</code>’s normal arrow-key browser, except it’s a frozen snapshot of a directory that may no longer exist. I poke around, confirm the folders I expect are present with sensible sizes, and only <em>then</em> am I happy to delete the original.</p> <h2 id="notes-to-self-learned-the-hard-way">Notes to self (learned the hard way)</h2> <ul> <li><strong>A scan that “finishes” suspiciously early is a red flag, not a relief.</strong> The danger is a silently truncated index that still looks complete when browsed — I can’t see what isn’t there.</li> <li><strong>Sanity-check completeness by counting entries.</strong> The number of items in the index, <code class="language-plaintext highlighter-rouge">zstdcat backup-index.ncdu.zst | grep -o '"name":' | wc -l</code>, should match <code class="language-plaintext highlighter-rouge">find /path/to/dir -xdev | wc -l</code> (both include the root directory; ncdu’s on-screen <code class="language-plaintext highlighter-rouge">Items:</code> count is exactly one less, which is fine).</li> </ul> <p>That’s the whole procedure: one environment, one command to capture, one command to browse. Small file, big peace of mind.</p> <h2 id="appendix-searching-the-index-by-regex">Appendix: searching the index by regex</h2> <p>To pick a particular file or folder out of the snapshot without clicking through the browser, I use the small Python script at the very end of this file. It walks the export, rebuilds the full path of every entry, and prints the ones whose path matches a regular expression — with sizes, so I can immediately judge what’s worth restoring. I save it as <code class="language-plaintext highlighter-rouge">ncdu_grep.py</code> (see bottom of page) and feed it the decompressed index on stdin.</p> <p>For example, to hunt down a MATLAB results file — one whose name starts with <code class="language-plaintext highlighter-rouge">result</code> and ends in <code class="language-plaintext highlighter-rouge">.mat</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>zstdcat backup-index.ncdu.zst | python3 ncdu_grep.py <span class="s1">'result.*\.mat$'</span>
</code></pre></div></div> <p>The pattern <code class="language-plaintext highlighter-rouge">result.*\.mat$</code> is a <em>regular expression</em> (not a shell glob), so each symbol carries a specific meaning:</p> <ul> <li><code class="language-plaintext highlighter-rouge">.</code> matches any single character.</li> <li><code class="language-plaintext highlighter-rouge">*</code> means “zero or more of the item immediately before it”, so the pair <code class="language-plaintext highlighter-rouge">.*</code> matches any run of characters — here, whatever sits between <code class="language-plaintext highlighter-rouge">result</code> and the extension.</li> <li><code class="language-plaintext highlighter-rouge">\</code> escapes the character that follows it: <code class="language-plaintext highlighter-rouge">\.</code> therefore matches a literal dot, instead of the “any character” that a bare <code class="language-plaintext highlighter-rouge">.</code> would otherwise mean.</li> <li><code class="language-plaintext highlighter-rouge">$</code> anchors the match to the <em>end</em> of the path, so only entries that genuinely end in <code class="language-plaintext highlighter-rouge">.mat</code> are returned (not, say, <code class="language-plaintext highlighter-rouge">result_old.mat.bak</code>).</li> </ul> <p>In short: a literal <code class="language-plaintext highlighter-rouge">result</code>, then any characters, then a literal <code class="language-plaintext highlighter-rouge">.mat</code> right at the end. You can find the script below:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="sh">"""</span><span class="s">Search an ncdu JSON export (read on stdin) by regex over full paths.

Usage:
    zstdcat backup-index.ncdu.zst | ncdu_grep.py </span><span class="sh">'</span><span class="s">REGEX</span><span class="sh">'</span><span class="s">
    zstdcat backup-index.ncdu.zst | ncdu_grep.py -i </span><span class="sh">'</span><span class="s">session0[12]</span><span class="sh">'</span><span class="s">
</span><span class="sh">"""</span>
<span class="kn">import</span> <span class="n">sys</span><span class="p">,</span> <span class="n">re</span><span class="p">,</span> <span class="n">json</span><span class="p">,</span> <span class="n">posixpath</span>

<span class="c1"># Parse command-line arguments, support optional case-insensitive matching.
</span><span class="n">args</span> <span class="o">=</span> <span class="n">sys</span><span class="p">.</span><span class="n">argv</span><span class="p">[</span><span class="mi">1</span><span class="p">:]</span>
<span class="n">flags</span> <span class="o">=</span> <span class="mi">0</span>
<span class="k">if</span> <span class="n">args</span> <span class="ow">and</span> <span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">==</span> <span class="sh">"</span><span class="s">-i</span><span class="sh">"</span><span class="p">:</span>            <span class="c1"># optional case-insensitive flag
</span>    <span class="n">flags</span> <span class="o">=</span> <span class="n">re</span><span class="p">.</span><span class="n">IGNORECASE</span>
    <span class="n">args</span> <span class="o">=</span> <span class="n">args</span><span class="p">[</span><span class="mi">1</span><span class="p">:]</span>
<span class="n">pat</span> <span class="o">=</span> <span class="n">re</span><span class="p">.</span><span class="nf">compile</span><span class="p">(</span><span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">flags</span><span class="p">)</span>

<span class="c1"># Load the ncdu JSON export from stdin and grab the directory tree.
# The JSON format used by ncdu stores the directory tree as the 4th element.
</span><span class="n">tree</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">sys</span><span class="p">.</span><span class="n">stdin</span><span class="p">)[</span><span class="mi">3</span><span class="p">]</span>

<span class="k">def</span> <span class="nf">human</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Format a byte count as a human-readable size string.

    The function scales the value through B, K, M, G, T units, using 1024 as
    the scaling factor. If the value exceeds the T range, it falls back to P.
    </span><span class="sh">"""</span>
    <span class="k">for</span> <span class="n">u</span> <span class="ow">in</span> <span class="p">(</span><span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">K</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">M</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">G</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">T</span><span class="sh">"</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">n</span> <span class="o">&lt;</span> <span class="mi">1024</span><span class="p">:</span>
            <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">n</span><span class="si">:</span><span class="mf">6.1</span><span class="n">f</span><span class="si">}{</span><span class="n">u</span><span class="si">}</span><span class="sh">"</span>
        <span class="n">n</span> <span class="o">/=</span> <span class="mi">1024</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">n</span><span class="si">:</span><span class="mf">6.1</span><span class="n">f</span><span class="si">}</span><span class="s">P</span><span class="sh">"</span>


<span class="k">def</span> <span class="nf">walk</span><span class="p">(</span><span class="n">node</span><span class="p">,</span> <span class="n">prefix</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Recursively walk the ncdu directory tree and print matching paths.

    `node` is a directory node from the ncdu JSON tree. The first element is a
    dict with the directory info, and the remaining entries are either child
    directories (lists) or file objects.
    </span><span class="sh">"""</span>
    <span class="n">info</span> <span class="o">=</span> <span class="n">node</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>                      <span class="c1"># first element describes the dir itself
</span>    <span class="n">path</span> <span class="o">=</span> <span class="n">info</span><span class="p">[</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">]</span> <span class="k">if</span> <span class="ow">not</span> <span class="n">prefix</span> <span class="k">else</span> <span class="n">posixpath</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">prefix</span><span class="p">,</span> <span class="n">info</span><span class="p">[</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">])</span>

    <span class="c1"># If the directory path itself matches the regex, print it with trailing '/'.
</span>    <span class="k">if</span> <span class="n">pat</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">path</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="nf">human</span><span class="p">(</span><span class="n">info</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">dsize</span><span class="sh">'</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span><span class="si">}</span><span class="s">  </span><span class="si">{</span><span class="n">path</span><span class="si">}</span><span class="s">/</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">child</span> <span class="ow">in</span> <span class="n">node</span><span class="p">[</span><span class="mi">1</span><span class="p">:]:</span>
        <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">child</span><span class="p">,</span> <span class="nb">list</span><span class="p">):</span>     <span class="c1"># a list =&gt; subdirectory: recurse
</span>            <span class="nf">walk</span><span class="p">(</span><span class="n">child</span><span class="p">,</span> <span class="n">path</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>                           <span class="c1"># an object =&gt; file
</span>            <span class="n">cpath</span> <span class="o">=</span> <span class="n">posixpath</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">path</span><span class="p">,</span> <span class="n">child</span><span class="p">[</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">])</span>
            <span class="k">if</span> <span class="n">pat</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">cpath</span><span class="p">):</span>
                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="nf">human</span><span class="p">(</span><span class="n">child</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">dsize</span><span class="sh">'</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span><span class="si">}</span><span class="s">  </span><span class="si">{</span><span class="n">cpath</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Start recursive traversal from the root of the tree.
</span><span class="nf">walk</span><span class="p">(</span><span class="n">tree</span><span class="p">,</span> <span class="sh">""</span><span class="p">)</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="how-to"/><category term="Unix"/><category term="bash"/><category term="archive"/><summary type="html"><![CDATA[this is what included plotly.js code could look like]]></summary></entry></feed>